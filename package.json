{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch src/server.js",
    "generate": "npx prisma generate",
    "migrate": "npx prisma migrate dev",
    "studio": "npx prisma studio",
    "start": "node dist/server.js",
    "build": "npx prisma generate && esbuild src/server.js --bundle --platform=node --target=node20 --outfile=dist/server.js --format=esm --external:@prisma/client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "prisma": "^7.8.0",
    "tsup": "^8.5.1"
  }
}
